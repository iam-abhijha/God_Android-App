<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mrtechy.god">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_main"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_main"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="abhi.app.clan.ComingSoon"></activity>
        <activity android:name="abhi.app.clan.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="abhi.app.clan.MainActivity"></activity>
    </application>

</manifest>